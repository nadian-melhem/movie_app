import 'package:flutter/widgets.dart';

import 'movie.dart';

class MovieListt extends StatelessWidget {
  List<Movie> movies;
  MovieListt({this.movies});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
